import { createElement } from "react";
import { renderToStaticMarkup } from "react-dom/server";
import { describe, expect, it } from "vitest";
import { LandingPage } from "../components/public/landing-page";

describe("LandingPage", () => {
  it("renders the public 3D landing page narrative, access model, and legal footer", () => {
    const html = renderToStaticMarkup(createElement(LandingPage));

    expect(html).toContain("argos-3d-page");
    expect(html).toContain("text-[var(--forge-text)]");
    expect(html).toContain("argos-scene-fallback");
    expect(html).toContain("<h1>Argos</h1>");
    expect(html).toContain("Turn every sales call into the next practice plan.");
    expect(html).toContain("Revenue Command");
    expect(html).toContain("The call becomes the coaching plan.");
    expect(html).toContain("Bring calls into one record");
    expect(html).toContain("Zoom");
    expect(html).toContain("Score the way your team sells");
    expect(html).toContain("Show the moments worth coaching");
    expect(html).toContain("Turn gaps into practice");
    expect(html).toContain("One thread from call review to practice.");
    expect(html).toContain("Call source");
    expect(html).toContain("Transcript");
    expect(html).toContain("Scorecard");
    expect(html).toContain("Highlights");
    expect(html).toContain("Training");
    expect(html).toContain("Roleplay");
    expect(html).toContain("Coach from what happened, not from memory.");
    expect(html).toContain("GoHighLevel workflow automation");
    expect(html).toContain("recording consent");
    expect(html).toContain("Start with one rep. Add the team later.");
    expect(html).toContain("Access platform");
    expect(html).toContain("Book a demo");
    expect(html).toContain("Privacy Policy");
    expect(html).toContain('href="#features"');
    expect(html).toContain(">Calls</a>");
    expect(html).toContain('href="#detail"');
    expect(html).toContain(">Coaching</a>");
    expect(html).toContain('href="#trust"');
    expect(html).toContain(">Team</a>");
    expect(html).toContain('href="#access"');
    expect(html).toContain(">Pricing</a>");
    expect(html).toContain('id="platform"');
    expect(html).toContain('id="features"');
    expect(html).toContain('id="detail"');
    expect(html).toContain('id="trust"');
    expect(html).toContain('id="access"');
    expect(html).toContain('href="/privacy-policy"');
    expect(html).toContain('href="/terms-of-service"');
    expect(html).toContain('href="/security-policy"');
    expect(html).toContain("© 2026 Argos Intelligence. All rights reserved.");
    expect(html).toContain("Solo");
    expect(html).toContain("$79/month");
    expect(html).toContain("120 live voice minutes/month");
    expect(html).toContain("Save 10% annually");
    expect(html).toContain("Team");
    expect(html).toContain("$50/seat/month");
    expect(html).toContain("120 live voice minutes per seat/month");
    expect(html).toContain("3-seat minimum");
    expect(html).toContain("Pooled at the org level");
    expect(html).toContain('action="/billing/checkout"');
    expect(html).toContain('name="seats"');
    expect(html).toContain('min="3"');
    expect(html).toContain('value="3"');
    expect(html).toContain('value="team" name="plan"');
    expect(html).toContain('value="team-annual" name="plan"');
    expect(html).toContain("250 extra minutes");
    expect(html).toContain("$125");
    expect(html).toContain("500 extra minutes");
    expect(html).toContain("$175");
    expect(html).toContain("2,000 extra minutes");
    expect(html).toContain("$600");
    expect(html).toContain("Included minutes are used first");
    expect(html).toContain("do not expire while subscribed");
    expect(html).toContain('href="/billing/checkout?plan=solo"');
    expect(html).toContain('href="/billing/checkout?plan=solo-annual"');
    expect(html).toContain('href="/billing/checkout?plan=extra-250"');
    expect(html).toContain('href="/billing/checkout?plan=extra-500"');
    expect(html).toContain('href="/billing/checkout?plan=extra-2000"');
    expect(html).toContain("Monthly checkout");
    expect(html).toContain("Annual checkout");
    expect(html).not.toContain(">Trial<");
    expect(html).not.toContain("Intelligent Readiness");
    expect(html).not.toContain("Ready Score");
  });
});
